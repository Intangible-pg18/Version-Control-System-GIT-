Introduction-

Set up an account, logg in, create a repository, make changes on the local machine, and push changes back to the remote repository. Use these git operations to share
changes from the remote repository to the local repository and vice-versa.

********************
Creating a private repo on github and copying the HTTPS link of the repo.

********************
Git operations-
git clone [URL]
cd directory_name

********************
Configure Git-
git config --global user.name "Prateek"
git config --global user.email "iamprateek999@.com"

********************
Edit the file and add it to the repository-
nano README.md -> I am editing the README file. Adding some more details about the project description.
git status
git add README.md
git commit -> I am editing the README file.
git push origin main

********************
Create a new file and commit it to the repository-
nano example.py -> def git_opeation():
                      print("I am adding example.py file to the remote repository.")
                   git_opeation()
git add example.py
git commit

********************
Add an empty file to the repository through web UI-
git push origin main -> ERROR!!!
git pull origin main
git push origin main
